/**
 * Objects - Logo Node Types
 */

/**
 * @typedef {object} LogoArgs
 * @prop {string} [size]
 * @prop {boolean} [link]
 * @prop {string} [theme]
 * @prop {string} [classes]
 */
export interface LogoArgs {
  size?: string
  link?: boolean
  theme?: string
  classes?: string
}
